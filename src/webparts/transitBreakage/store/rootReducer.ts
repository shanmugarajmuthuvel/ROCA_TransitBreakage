import { combineReducers } from "@reduxjs/toolkit";

export const rootReducer = combineReducers({
  // Add web part-scoped slices here as features are built out
  // example: transitBreakage: transitBreakageReducer,
});
